{
	"private": true,
	"name": "address",
	"main": "dist/index.js",
	"scripts": {
		"build": "rm -r dist && tsc"
	},
	"devDependencies": {
		"typescript": "^5.1.6",
		"vitest": "^0.30.1"
	},
	"dependencies": {
		"uint8array-utils": "workspace:^"
	}
}
