import { describe, expect, test } from "vitest";
import { ByteArray } from "../lib/binary/byte-array";

describe("Byte Array", () => {

    test("to number", () => {
        let byteArray = new ByteArray(265)

        expect(true).toBe(false)
    })

})